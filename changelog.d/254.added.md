Compiler `TemplateWarning` findings now flow through `kida check`, `diagnose_source()`, and `diagnose_directory()` with deterministic text, diagnostics JSON v1, and SARIF output while retaining stable locations, suggestions, confidence, and advisory-only edits.
