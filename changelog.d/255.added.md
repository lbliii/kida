K-WARN-002 now detects a Jinja `{% set %}` whose binding is read after an
`if` block, including nested branches, named blocks, and reads later in the
same loop iteration. The advisory remains default-on, emits no safe edit, and
does not claim that loop-local assignments escape their loop.
