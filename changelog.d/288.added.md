Added opt-in, policy-neutral template shape profiles under `kida.analysis`.
Immutable records and source/template entry points expose deterministic volume,
depth, control-flow, loop, dynamic-expression, component-call, slot, literal
attribute, dependency, repetition, fingerprint, and exact ownership-span facts
without adding advice, a score, CLI behavior, or top-level exports.
