Shared `DependencyWalker`, `PurityAnalyzer`, and `BlockAnalyzer` instances now isolate traversal state across concurrent calls. Thread-safety documentation also defines the stable-source boundary for shared loaders and the read-only boundary for shared metadata mappings.
