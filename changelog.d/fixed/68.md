Partial evaluator no longer folds non-constant-safe types (dict, list, set, custom objects) into AST `Constant` nodes, which caused `TypeError` from Python's `compile()`. Intermediate dict/list values from `static_context` are now kept in the evaluator's context for downstream folding but are not emitted as `Const` nodes.
