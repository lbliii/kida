Partial evaluator now partially transforms `{% let %}` / `{% set %}` value expressions when the value cannot be fully resolved, preventing `UndefinedError` for loop variables inside unrolled `{% for %}` loops.
