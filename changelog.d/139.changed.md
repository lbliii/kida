GitHub pull-request release-note collection now runs through typed, fixture-tested Python instead of an inline program in `action.yml`. Collection follows pagination, validates its report contract, and reports malformed GitHub responses or invalid ref ranges explicitly.
