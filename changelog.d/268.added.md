Blocks and fragments now accept ordered `name=literal` modifiers. Immutable
`BlockModifierMetadata` records preserve scalar types and source locations for
framework introspection without changing rendering or structural block hashes.
